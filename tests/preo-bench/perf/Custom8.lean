import Uwueave.Preo.Elab

namespace PreoBench.Custom8

open Uwueave Uwueave.Preo

preo Subject where
  field f00 : (custom (Catalog.GSet Nat)) := fun _ => false
  field f01 : (custom (Catalog.GSet Nat)) := fun _ => false
  field f02 : (custom (Catalog.GSet Nat)) := fun _ => false
  field f03 : (custom (Catalog.GSet Nat)) := fun _ => false
  field f04 : (custom (Catalog.GSet Nat)) := fun _ => false
  field f05 : (custom (Catalog.GSet Nat)) := fun _ => false
  field f06 : (custom (Catalog.GSet Nat)) := fun _ => false
  field f07 : (custom (Catalog.GSet Nat)) := fun _ => false

end PreoBench.Custom8
