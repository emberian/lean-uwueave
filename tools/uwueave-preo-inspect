#!/usr/bin/env bash
set -euo pipefail

tool_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
repo_dir=$(dirname -- "$tool_dir")
cd "$repo_dir"
exec lake env lean --run Uwueave/Preo/ArtifactInspectionMain.lean "$@"
